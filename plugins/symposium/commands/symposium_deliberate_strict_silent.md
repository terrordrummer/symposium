---
description: Symposium deliberation — STRICT (fixed panel) + SILENT (sync). Plainest mode.
---

Invoke the **`mcp__symposium__deliberate_muted`** MCP tool with:

- `problem`: the text between the markers below, verbatim
- `provider`: `"cli-auto"`

```
$ARGUMENTS
```

`_strict_silent`: fixed six-persona panel, no live streaming, one sync result at the end. The plainest mode — useful for scripting or when you just want the final answer.

Dispatch the call exactly as above and report: `outcome`, `synthesis_answer` (if any), `transcript_digest`, `run_dir`, `cumulative_usage`, `rounds`.
